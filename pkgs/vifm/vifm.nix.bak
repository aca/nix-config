{
  config,
  pkgs,
  lib,
  ...
}: {
  # environment.extraInit = lib.mkAfter ''
  #   export MYVIFMRC=${
  #     pkgs.writeTextFile {
  #       name = "my-file";
  #       text =
  #         (builtins.readFile ./pkgs/vifm/vifmrc)
  #         + (
  #           if pkgs.stdenv.isDarwin
  #           then (builtins.readFile ./pkgs/vifm/vifmrc-osx)
  #           else (builtins.readFile ./pkgs/vifm/vifmrc-linux)
  #         );
  #     }
  #   }
  # '';
}
