# Do not modify this file!  It was generated by ‘nixos-generate-config’
# and may be overwritten by future invocations.  Please make changes
# to /etc/nixos/configuration.nix instead.
{
  config,
  lib,
  pkgs,
  modulesPath,
  ...
}: {
  imports = [
    (modulesPath + "/installer/scan/not-detected.nix")
  ];

  boot.initrd.availableKernelModules = ["xhci_pci" "ahci" "nvme" "usbhid" "usb_storage" "sd_mod"];
  boot.initrd.kernelModules = [];
  boot.kernelModules = ["kvm-intel"];
  boot.extraModulePackages = [];

  fileSystems."/" = {
    device = "/dev/disk/by-uuid/d6ac3c2f-a884-4088-bdf6-93804aed5eb4";
    fsType = "ext4";
  };

  fileSystems."/boot" = {
    device = "/dev/disk/by-uuid/4144-62D2";
    # device = "/dev/disk/by-uuid/2D17-DC19";
    fsType = "vfat";
    options = ["fmask=0022" "dmask=0022"];
  };

  swapDevices = [];

  # fileSystems."/mnt/nas" = {
  #   device = "192.168.0.16:/volume1/root";
  #   fsType = "nfs";
  #   options = ["noatime" "x-systemd.requires=network-online.target"];
  # };

  nixpkgs.hostPlatform = lib.mkDefault "x86_64-linux";
  # hardware.cpu.intel.updateMicrocode = lib.mkDefault config.hardware.enableRedistributableFirmware;

  environment.sessionVariables = { LIBVA_DRIVER_NAME = "iHD"; }; # ditto

  hardware.graphics = {
    enable = true;
    extraPackages = with pkgs; [
      # intel-ocl
      # vpl-gpu-rt          # for newer GPUs on NixOS >24.05 or unstable
      # intel-vaapi-driver
      # intel-media-driver
      # intel-compute-runtime-legacy1 
      # onevpl-intel-gpu  # for newer GPUs on NixOS <= 24.05
      # intel-media-sdk   # for older GPUs
    ];
  };
}
